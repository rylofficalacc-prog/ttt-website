"use client"; import {useState} from "react"; import {useRouter} from "next/navigation"; import {players} from "../lib/data"; export default function Search(){const[q,setQ]=useState("");const[e,setE]=useState("");const r=useRouter();function go(x){x.preventDefault();const p=players.find(v=>v.username.toLowerCase()===q.trim().toLowerCase());if(!p)return setE("Player not found in the demo rankings.");r.push(`/players/${p.username}`)}return <><form className="search" onSubmit={go}><input value={q} onChange={x=>setQ(x.target.value)} placeholder="Search a ranked player..."/><button>Search</button></form>{e&&<small className="error">{e}</small>}</>}